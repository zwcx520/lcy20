    // 通过id获取页面上已存在的div元素
    const targetTitle = document.getElementById('targetTitle');
    if (targetTitle) {
      // 创建一个文本节点
      const textNode = document.createTextNode('不要奢望能够赢得所有人的好感，因为那几乎是不可能的的事情；按照平常自己待人接物的处理方式对待你遇到的每一个人就足够了！ᖰ˃̶ ꇴ ˂̶ᖳ      ');
      // 将文本节点添加到div中
      targetTitle.appendChild(textNode);
    }
